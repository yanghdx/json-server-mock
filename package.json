{
    "name": "json-server-mock",
    "version": "0.0.1",
    "description": "json-server-mock",
    "scripts": {
        "mock": "json-server --watch db.json --port 10000",
        "mock-sa": "json-server --watch db-sa.json --port 10000"
    },
    "repository": {
        "type": "git"
    },
    "dependencies": {
        "json-server": "^0.9.5"
    }
}